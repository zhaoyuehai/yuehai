<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yuehai.android.center">

    <application
        android:name="debug.CenterApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Center组件"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppThemeBase"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity android:name=".ui.UserInfoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".ui.service.DemoAIDLService"
            android:exported="true"
            android:process=":DemoAIDLService">
            <intent-filter>
                <action android:name="com.yuehai.android.center.ui.service.DemoAIDLService" />
            </intent-filter>
        </service>
    </application>
</manifest>